apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-webflux-demo.fullname" . }}
  labels:
    {{- include "api-webflux-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-webflux-demo.selectorLabels" . | nindent 4 }}
